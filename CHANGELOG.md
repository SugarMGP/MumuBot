# 更新日志

本项目所有重要变更都会记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

## [2.0.6] - 2026-08-06

### 修复

- **长期记忆减少普通聊天和同义重复**：话题摘要严格区分稳定事实与最近对话过程，保留旧事实时不再反复改写；长期记忆提取默认忽略一次性发言、情绪和玩笑，并合并同一命题的重复表述。
- **长期记忆跨分类去重**：同群语义候选不再被范围、类型或主体字段过早过滤，合并判断会核对元数据，并以新记忆的规范化分类保存结果。
- **DeepSeek 工具调用参数更稳定**：模型将整数或布尔参数误写成字符串时，会依据工具参数定义做保守矫正，减少格式差异导致的工具调用失败。
- **戳一戳现已显示成员昵称**：交互事件会复用群名片、昵称和成员信息缓存补全发起者与目标，查询失败时才回退 QQ 号。
- **结构化模型输出解析更稳**：正常 JSON 会原样保留，格式异常时清理代码块、尾部字符等语法噪声后再按目标结构反序列化，无法匹配时仍然报错。
- **成员画像减少单句误学**：学习时会结合已有画像输出完整结果，新口癖、说话风格和兴趣至少需要两条直接证据，并提供消息时间供模型识别临时刷屏；学习批次、审核周期和消息缓冲的代码默认值同步最新示例配置。
- **聊天表达减少梗滥用和重复**：人格提示进一步要求结合语境使用流行梗、控制拌嘴攻击性，并在发言前避免复述已有内容。
- **话题列表详情箭头恢复显示**：修正模板组件被当作普通文字输出的问题，“查看详情”不再显示内部图标调用文本。
- **发布工作流更新官方 Actions**：升级 Node、Go、Checkout 及 Docker 构建相关 Actions，消除 Node.js 20 运行时弃用告警。

## [2.0.5] - 2026-08-05

### 新增

- **管理后台统一使用 Lucide 图标与组件库阴影**：后台全部图标迁移到 Lucide 线性图标，卡片统一使用 daisyUI 深度与阴影层级，页面层级更清晰一致。
- **首页心情改用原生进度条**：移除 ECharts 依赖，前端资源体积明显下降，减少动态效果时进度条仍正常显示。

### 变更

- **首次看到图片即可理解内容**：图片与视频识别改回同步解析，机器人第一次收到图片时就能在思考中看到图片内容，不再等待后台补全。
- **群消息处理改为并行解析、按到达顺序落库**：去掉同群串行队列，消息解析（含图片识别）互不阻塞；消息、戳一戳和撤回按到达顺序统一提交，落库、话题系统与思考缓冲顺序一致。
- **speak 改回一次一条消息**：每次调用只发送一条消息，多说几句通过多次调用表达，聊天节奏更自然。

### 修复

- **管理后台细节修正**：系统状态页消除卡片空档、表情包预览入口去重、文件名不再作为卡片主标题、提交失败后按钮图标完整恢复、长标题不再挤压状态标签。

## [2.0.3] - 2026-08-04

### 变更

- **后台恢复扫描不再占用实时思考节拍**：话题摘要和长期记忆下沉改用独立的 `learning.recovery_interval` 配置，默认每 180 秒扫描一次，避免失败任务按聊天思考频率重复请求模型。
- **话题摘要输出约束更明确**：摘要提示明确要求始终返回非空的标题和概述，空结果继续保留待处理状态。

### 修复

- **预期的数据库未命中不再刷 GORM 错误日志**：初始化数据库连接时忽略 `record not found` 的默认日志输出，同时保留业务层对真实错误的处理。
- **人格与结构化输出提示保持最新约束**：调整默认人格表达和 JSON 输出说明，减少机械回复与不符合格式的模型响应。

## [2.0.2] - 2026-08-03

### 变更

- **持续集成与发布构建不再重复执行**：`main` 和 Pull Request 只运行单次构建与静态检查，完整平台矩阵仅在版本 tag 发布时执行；多架构容器改用原生构建平台交叉编译，共享前端资源、templ 和依赖缓存，减少 QEMU 下的重复慢构建。

### 修复

- **话题和长期记忆的文本召回不再因上下文片段报错**：混合检索统一使用 PostgreSQL 类型化文本数组参数，单条或多条原始消息都不会再被 ORM 展开成无效的 `unnest` 调用。
- **群聊上下文中的提及恢复为可读称呼**：提及机器人会显示为“@机器人名字(你)”，提及群成员会优先显示事件中的称呼，并在后台补全群名片或昵称，不再长期保留为 QQ 号。

## [2.0.1] - 2026-08-03

### 变更

- **机器人账号改由 OneBot 自动识别（破坏性变更）**：每次连接会通过 `get_login_info` 获取当前 QQ，并统一用于消息渲染、群文化学习和话题记忆；人格配置与静态提示词不再保存 QQ，升级已有配置时必须删除 `persona.qq`。
- **默认人格会先理解对方真实表达再接话**：补充语境与情绪理解约束，减少只抓表面关键词或在信息不足时强行参与的回复。
- **思考期间的新消息改为延后处理**：同群已有 ReAct 正在运行时会忽略新的思考触发，不再自动追加 rerun；消息仍会保留，并在后续正常触发的固定快照中统一处理，避免连续发起模型请求。
- **同群消息不再被展示补全阻塞**：消息解析、去重入库、撤回和缓冲顺序继续串行保证一致性，标已读、回复与转发补全、视觉识别和成员画像改为后台处理；思考防抖从 OneBot 接收事件时开始计算，慢补全不会推迟后续消息聚合或改变既有顺序。
- **回复决策收敛为单次 ReAct**：移除主 ReAct 前的轻量模型分类调用，强交互继续直接进入思考，普通消息保留概率和频率门控，最终统一由 ReAct 发言、行动或保持沉默。
- **话题和主动记忆改用原始上下文召回**：固定快照中的原始消息会共用一次 embedding，并通过 pgvector、逐条 pg_trgm 匹配和 RRF 召回历史话题及长期记忆，不再依赖模型生成检索词或额外关键词提取。
- **群聊表达方式改为按需查询**：Agent 需要接梗或贴合本群说法时可主动查询已审核的场景、表达总结和少量原始示例；示例继续使用消息证据关系保存，并受当前群、撤回状态和思考快照限制，不再每轮自动注入提示词。

### 修复

- **OneBot 断线不再反复初始化或刷屏**：首次连接失败和运行中断线统一在当前进程内立即尝试连接，失败后按既有间隔后台重试，数据库、模型、学习任务和后台只初始化一次；重复失败降为调试日志，首次创建学习游标也不再打印预期的未找到记录。

## [2.0.0] - 2026-08-03

> [!WARNING]
> **Breaking Change**：v2.0.0 不兼容 1.x 的数据库、模型配置与部署方式。长期记忆、话题、学习和后台数据改为按全新 PostgreSQL/pgvector 数据库初始化，不提供 MySQL、Milvus 或旧表迁移；模型配置由高、中、低三档收敛为 high/low 两档，并启用严格配置校验。升级前请备份旧数据，并按最新示例重新配置和部署。

### 新增

- **支持 Docker Compose 一键部署**：新增多阶段构建镜像，并自动启动 MumuBot、PostgreSQL/pgvector 和 NapCat；首次启动会生成缺失的运行配置且不覆盖已有文件，NapCat 会自动启用供 MumuBot 使用的内部正向 WebSocket，数据库、NapCat 配置、QQ 登录和表情包分别持久化，模型端点、外部 OneBot 地址与后台密钥可由环境变量提供，本地表情包以内联数据发送，不依赖容器共享文件路径。
- **群聊上下文现可感知戳一戳与消息撤回**：戳机器人会作为强互动进入现有思考链路，戳其他人和机器人自身的戳一戳回显只补充当前上下文；群消息被撤回后会清除正文、转发和回复信息，只保留撤回占位，并停止参与尚未完成的话题、学习和记忆处理。

### 变更

- **管理后台完成轻量化界面重构**：统一采用亮色 Quiet Sakura 视觉、圆角卡片、daisyUI 控件、HTMX 局部刷新和 ECharts 心情图表，重做首页、审核、话题、记忆、成员、表情包与系统状态页面，并补齐原生弹窗、筛选反馈和操作提示。
- **发布流程会同步推送容器镜像**：创建版本 Release 前会构建 Linux AMD64 和 ARM64 镜像并推送到 GitHub Container Registry，同时发布版本号、主次版本和 latest 标签。
- **语义存储已统一到 PostgreSQL**：长期记忆、话题摘要、群文化、成员画像、表情包和情绪状态统一使用 PostgreSQL，向量检索采用 pgvector 精确余弦与 pg_trgm 经 RRF 融合，不再依赖 MySQL 和 Milvus。
- **PostgreSQL 扩展随应用启动启用**：应用会在迁移前自动执行 `CREATE EXTENSION IF NOT EXISTS` 启用 vector 和 pg_trgm，Docker 与直接运行共用同一条初始化链路；服务端仍需预装 pgvector，并为运行账户提供相应权限或由管理员预先创建扩展。
- **模型配置收敛为高低两档**：实时 ReAct 使用高档模型，原中档承担的话题处理和群文化学习统一改用轻量模型；两档对话模型及 Embedding、视觉模型均可通过环境变量完整覆盖 Base URL、API Key 和模型名，容器时区固定为上海。
- **管理后台默认端口调整为 7468**：直接运行和 Docker Compose 使用同一默认端口，仍可通过配置或 `MUMU_WEB_PORT` 覆盖宿主机映射。
- **话题与学习链路改为可追溯水位**：话题摘要按归属完成顺序保存不可变版本，空摘要不会推进水位，自动长期记忆的参与者候选受同一摘要水位约束，失败会保持待处理并按既有思考周期重试；learner 只消费已完成话题判定的连续前缀，并以群文化和成员画像两个游标独立推进。
- **话题沉淀和群文化积压处理更完整**：摘要更新会保留仍成立的旧事实与未完事项，自动长期记忆不再静默截断摘要内的有效条目；learner 启动后会立即处理一次已有积压。
- **Agent 使用固定消息快照思考**：上下文明确区分已读与本轮消息，分类失败和未实际执行动作的 ReAct 失败不会误推进已读位置，普通触发仍沿用既有概率门控。
- **消息表情回应改为语义选项**：Agent 现在从赞、爱心、鼓掌、笑、拥抱、疑问、惊讶、哭、捂脸、庆祝、敬礼和 doge 等常用选项中选择回应，不再直接填写底层表情编号。
- **OneBot 接入迁到 napcat-sdk**：依赖已升级到 v1.0.3 并移除旧 Resty 间接依赖；保留内部 Adapter 的自动重连、群内保序慢路径隔离、原始 notice 解析、禁言状态、成员详情缓存和动态返回体校验，并继续暴露现有 OneBot 11 业务接口。
- **Agent 单次发言现可发送多条消息**：一次 `speak` 可按顺序发送一至五条文字，每条可独立引用消息和提及群友，并会在发送前完成整批校验，减少同轮反复调用工具造成的机械感。
- **群聊思考与提示词已收紧**：普通群聊保持按批次采样既有发言频率，并修正零频率被防话痨下限抬高的问题；默认人格删除成组矫正案例和机械格式限制，将冗长行为禁令收敛为参与时机、表达、关系和事实边界，并参考 MaiBot 当前提示词强化话题把握、参考信息取舍和自然表达重组；动态上下文也不再暴露农历生肖、情绪数值、记忆访问次数及风格卡片原始语料。
- **运行环境要求已提升至 Go 1.26.5**：源码构建会使用包含安全修复的 Go 版本，降低标准库已知漏洞带来的风险。
- **配置加载现会在启动时严格校验**：未知配置项、额外 YAML 文档和无效的间隔、端口、概率、向量参数会明确报错，避免运行阶段才触发异常。
- **人格模板现会在启动时完成校验**：模板语法或字段引用错误会作为配置错误返回，不再延迟到首次思考时中断进程。
- **重复工具调用只会在首次成功后跳过**：临时失败的工具调用可由模型在同一轮重试，避免错误地报告为已成功执行。
- **内部通用代码已进一步收紧**：移除未使用的人格访问接口、话题与后台的透传结构、静态资源帮助函数和未被消费的摘要元数据，合并重复的 RRF 排名转换逻辑，减少维护面。
- **运行状态与初始化路径已收敛**：OneBot 重连、Agent 思考防重、短消息缓冲、话题模型和情绪单例各自只保留一套事实来源，并删除无调用的 API、字段和消息转换层，降低关闭竞态与长期维护成本。

### 移除

- **移除 1.x 存储实现与迁移兼容层**：不再连接 MySQL 或 Milvus，也不保留旧表、旧字段和过渡数据迁移；v2.0.0 按全新 PostgreSQL 数据库运行。
- **移除中档模型及旧配置别名**：话题、学习、分类和记忆等非实时任务统一使用 low 模型，旧的 mid 配置和过期环境变量不再读取。

### 修复

- **表情包默认存储目录保持一致**：配置未填写存储路径时统一使用 `./stickers`，自动保存、工具发送和后台文件访问不再指向不同目录。
- **群聊人物上下文不再重复查询成员特征**：每名近期成员只读取一次特征数据，减少每轮思考中的重复数据库查询。
- **高负载停机不再被 OneBot 群队列阻塞**：事件队列已满时，等待投递会响应连接关闭信号，避免停机流程在排空事件流前永久等待。
- **并发情绪更新不再相互覆盖**：情绪增量和定时衰减会在数据库事务中锁定同一状态行，避免多个群聊同时更新时丢失已提交变化。
- **OneBot 本地消息链路不再静默丢弃**：每群队列改为阻塞背压，关闭时按事件流、群队列和 Agent 回调顺序排空；消息先原子写入日志及空原文归属，再延迟获取回复、转发和视觉展示信息，重复事件不会重复触发统计与思考。
- **OneBot JSON 解析统一使用 Sonic**：事件分流、群消息和动态 API 返回体统一通过 Sonic 流式解析并保留整数精度，停机投递只保留真实背压与排空语义。
- **话题记忆与学习边界进一步收紧**：未完事项只能写入目标类长期记忆，代码会拒绝模型越权类型；learner 配置会阻止批次小于最少证据数，数据库和审核失败也会留下可观察日志。
- **群文化可随新证据重新审核**：被拒绝的黑话和风格模式在获得新的独立证据后会恢复为候选，已审核生效的内容保持不变。
- **重复风格候选不再重复生成向量**：已存在或同批重复的场景表达直接复用记录并追加证据，避免无意义的 embedding 调用及其失败阻断。
- **回复旧话题时会优先带入对应上下文**：话题归属提示明确标记被回复话题，ReAct 的固定快照也会优先装入相关话题，并限制话题上下文总长度，避免旧话题缺失或提示词无限增长。
- **成员时间和工具副作用不再被提前覆盖**：成员昵称、群名片和最近发言按消息真实时间单调更新；发言、戳一戳、贴表情、撤回和表情包只有成功后才标记已执行，失败的外部只读工具不会误推进快照。
- **长期记忆失败不再破坏旧内容或误推进消息**：claim 提取失败会保持摘要待处理，模型未返回完整合并正文时会中止写入，被判定无需保存的内容也不会再被记录为已执行动作。
- **思考期间的动态上下文保持固定快照**：话题列表、延迟归属摘要、关键原文和近期消息工具统一受本轮最后一条消息的数据库水位约束，新到消息不会提前混入当前回复。
- **长期记忆证据现可显式选择消息来源**：Agent 可传入聊天或工具返回的消息 ID 作为证据，包括自己的发言；未指定时仍回退本轮最后一条非机器人消息，避免 rerun 误绑缓冲区尾部。
- **强提及不会被模型故障拦截或遗忘**：被提及、点名或回复机器人时统一写入强交互标记，即使轻量分类超时或返回无效结果也会继续进入主 ReAct；主 ReAct 无副作用失败时会按思考周期重试，不再依赖无关的新消息触发。
- **普通群消息可稳定写入消息日志**：没有合并转发内容时使用空值保存可选 JSON 数据，不再因无效 JSON 阻断话题、学习和记忆链路。
- **后台系统信息不再展示内部存储参数**：系统页只保留连接、群聊和功能状态等使用者需要关注的信息。
- **语义证据与精确正文现由数据库保证一致**：长期记忆和群文化证据使用外键及唯一约束，完全相同的 active 记忆不会重复创建，删除来源时也不会遗失仍被引用的证据。
- **OneBot 大整数不再经过浮点数解析**：事件和动态 API 返回值使用标准 JSON 数字校验，避免较大的 QQ 号或消息编号发生精度损失。
- **异常群消息不会进入业务队列**：缺少有效群号、消息编号或发送者编号的事件会在 Adapter 边界被拒绝，避免形成无法推进的消息快照。
- **OneBot 消息发送现会校验消息编号**：服务端响应缺少、为零或格式错误的 `message_id` 会返回错误，避免后续把未确认的发送伪造成成功消息。
- **连接与后台任务现会正确处理竞争关闭**：OneBot 连接建立、断开和重连不会再让过期连接覆盖当前连接；关闭期间不会复活连接、新增群消息 worker 或追加思考任务，内存定时任务也会退出后再关闭数据库。
- **长期记忆现只接纳明确标记为长期的内容**：事实、偏好、约束、经历和目标在模型标记为短期时都会跳过，减少长期记忆污染。
- **思考防抖会停止被替换的旧计时器**：连续消息聚合时不会遗留无效定时任务。
- **图片下载会检查临时文件关闭结果**：写入未完整落盘时会明确失败，避免返回不完整文件。
- **媒体消息会遵守普通触发规则进入 ReAct**：图片、表情包和视频等只有展示内容的消息在通过既有概率门控后不再被提前标记已读；话题、检索和学习等语义链路仍只使用原始文本。
- **数据库写入失败不会再伪装成功**：后台审核、记忆状态、自动群文化审核和消息展示更新统一检查实际命中记录；失效操作会返回明确错误，消息展示写入失败也不会继续进入内存缓冲和 ReAct。
- **表情包删除保持数据库与文件状态一致**：先提交数据库记录删除，再清理本地文件；文件清理失败会记录告警，不再出现数据库回滚后文件已经丢失的破坏性状态。
- **话题摘要历史会完整展示增删变化**：详情页同时显示新增和移除的事实与待办，便于核对摘要演进，不再只展示新增内容。

### 安全

- **升级 PostgreSQL 驱动与文本处理依赖**：将 `github.com/jackc/pgx/v5` 升级到 5.9.2、`golang.org/x/text` 升级到 0.39.0，修复已知可达漏洞；发布工作流同时固定 templ 生成器版本，降低构建漂移。

## [1.3.10] - 2026-06-20

### 变更

- **消息日志清理不再等待后台学习进度**：旧消息清理只保留已归属话题的消息和每群最新消息，不再因为 learner 尚未处理而额外保留普通旧消息。
- **话题分配改为快速失败策略**：模型调用失败、漏项或返回非法归属结果时会直接标记为无话题，避免 pending 消息长期阻塞后台学习。
- **话题分配批量大小现与消息缓冲区一致**：批量归属不再使用消息缓冲区的一半作为上限，降低小缓冲区下频繁分批的开销。
- **话题分配任务队列容量已提升**：待分配消息队列从 256 扩大到 1024，降低高消息量时直接触发队列满处理的概率。

### 修复

- **成员画像学习现可正确写入学习结果**：后台成员画像学习改为注入学习上下文，避免工具误判为非学习阶段后反复失败。

## [1.3.9] - 2026-06-20

### 变更

- **仓库协作规则已扩展为分层知识库**：根目录和核心子目录补充了职责边界、入口文件和修改约束，后续排查话题、消息解析、学习、记忆和后台问题时更容易定位。

### 修复

- **空原文消息不再进入话题归属队列**：原始文本为空的消息会在写入消息日志后直接标记为无话题，启动补偿也会跳过这类消息的模型分配，避免空消息进入话题归属批处理。
- **异常话题归属结果不再伪装成无话题**：模型漏项、非法动作、无效候选话题或新话题编号缺失时会保留待处理状态并记录日志，避免把失败的归属结果写成最终无话题。
- **消息原文不再混入展示占位文本**：@ 成员、@ 全体成员、语音、文件、卡片和合并转发不再写入原始文本字段，仍会作为聊天上下文提示展示给 bot，避免污染话题归属、摘要和学习链路。

## [1.3.8] - 2026-06-04

### 变更

- **Agent 包已按职责拆分为多个文件**：原 1724 行的 `react_agent.go` 拆为 `react_agent.go`（核心定义+生命周期）、`message.go`（消息处理）、`think.go`（思考循环+执行）、`context.go`（上下文组装+媒体处理），`behavior_helpers.go` 合入 `message.go`。
- **Memory 包已按职责拆分为多个文件**：原 2204 行的 `manager.go` 拆为 8 个文件，分别承担记忆 CRUD、入库合并、样式卡、黑话、表情包、情绪状态、成员名记录和消息清理等职责。
- **OneBot 包已按职责拆分为多个文件**：原 1491 行的 `client.go` 拆为 `client.go`（连接+事件分发）、`client_types.go`（数据类型）、`client_api.go`（API 封装）、`client_parse.go`（消息解析+缓存）。
- **Topic 包已完成轻量清理**：纯渲染函数移入新 `prompt.go`，评分辅助函数移入 `scoring.go`，`manager.go` 从 1584 行缩减至 1385 行。
- **回复信息查找顺序已优化**：`resolveReplyInfo` 的缓存检查从数据库之后提前到数据库之前，减少不必要的数据库查询。
- **已清理长期记忆模块中的死代码**：移除了从未使用的 `MemoryOpenLoopGraceWindow` 常量、`RecallEligible` 方法、`SetMemberNameRecords` 方法、`MemberNamesSearchText` 函数和 `UpdateMemoryContent` 方法。
- **向量存储接口现已精简并改用原生 upsert**：移除了从未调用的 `DeleteByGroup` 和 `GetConfig` 接口方法及 Milvus 实现；记忆向量更新从先删后插改为 Milvus 原生 upsert，减少一次网络往返。
- **长期记忆更新已从 map 改为结构体直接操作**：`memoryWithUpdates` 60 行手动字段映射和 `updateMemoryWithVector` 的 map 入参模式已移除，改为 `updateMemoryFields` 闭包回调，新增 `buildFactKey` 也去掉了从未使用的类型参数。
- **Agent 关机逻辑已提取共享 `shutdown` 方法**：`Stop()` 和 `cleanupAfterInitFailure()` 的重复组件关闭代码已合并为单一 `shutdown()` 方法。
- **Content-Type 扩展名检测已内联**：`getExtensionFromContentType` 单调用函数已内联到 `DownloadImage`。
- **消息日志转换函数已去重**：`messageLogBaseGroupMessage` 在 `agent` 和 `topic` 两个包中的重复定义已合并为 `onebot.MessageLogToGroupMessage`。
- **OneBot 数值解析已统一**：自定义 `parseInt` 现委托给 `utils.ParseInt64Value`，消除重复的类型 switch。
- **显示名选取逻辑已统一**：`displayNameForRenderedText`、`displayNameFromNames`、`preferredGroupMemberDisplayName` 三个函数共用新的 `utils.FirstNonEmpty` 工具函数。
- **跨包重复工具函数已去重**：`uniqueMemoryIDs` / `uniqueTopicIDs` 合并为 `utils.UniqueIDs`；`buildFactKey` / `buildSummaryFactKey` 和 `normalizeMemoryContentForKey` / `normalizeContentForKey` 已导出为 `memory.BuildFactKey` / `memory.NormalizeContentForKey`，topic 包直接复用；`emptyDash` / `connectionText` 已从 views 包导出，app 包不再重复定义。
- **ToolContext 中已移除从未使用的 TopicID 字段**：该字段被赋值但从未被任何工具读取，已从结构体和所有赋值处清理。
- **重复防御性编程代码已清理**：`think()` 中 4 次 `getBuffer` 调用合并为 1 次并透传下游；`onMessage()`、`Start()`、`getSpeakProbability()`、`parseMessageContent()` 等函数中的重复 `config.Get()` 调用已提取为局部变量；`atDisplayName()` 和 `normalizeCanonicalType()` 中的重复 `TrimSpace` 已消除。
- **长期记忆入库状态赋值逻辑已简化**：`IngestMemory` 中 6 行条件链（4 行死代码）已简化为 2 行有效逻辑：非 migration 来源直接设为 Active，migration 来源设为 Legacy。
- **AGENTS.md 已补充 Go 代码规范约束**：新增"Go 代码结构"（文件行数上限、禁止重复调用）、"死代码"（发现即删）和"生成文件"（禁止手动修改）三个章节。

## [1.3.7] - 2026-06-01

### 变更

- **话题分配超时已延长至 90 秒**：启动补偿和运行时批量归属的 LLM 调用超时从 30 秒提升到 90 秒，减少 API 响应较慢时频繁触发 `context deadline exceeded` 的情况。
- **已移除未使用的话题分配排空超时常量**：`topicAssignDrainTimeout` 定义后从未被引用，现已清理。

## [1.3.6] - 2026-05-31

### 变更

- **README 与示例配置现已补充结构化输出要求**：模型配置说明会明确提示中档模型和轻量模型需支持 OpenAI 兼容的 `json_object` 结构化输出，减少选型后才发现话题摘要、上下文分类或长期记忆提取无法工作的情况。
- **若干单调用 helper 现已收口内联**：上下文分类窗口裁剪、回复自检和部分模型/视图层转发 helper 已做小范围清理，减少无实际复用价值的中转函数，后续阅读主链路时更直接。
- **启动补偿现会按群一次性清完旧 pending 消息**：每个群的待分配旧消息会在启动补偿里整体拉取并直接批量归属，不再按 `batchSize` 分批循环补偿，减少启动阶段反复补跑同一群历史积压的情况。
- **多条话题与检索热路径现已收紧数据库往返**：启动加载会批量拉取话题尾部消息并合并统计待摘要数量，批量话题归属和长期记忆向量命中后的访问计数也改为按批处理，减少高频链路里的逐条查库和逐条写库。
- **后台运行态与话题存储依赖现已进一步收口**：后台状态快照与话题存储桥接去掉了重复中转接口，后续维护运行态和话题主链路时更直接，也减少拆包后的重复接口定义。

### 修复

- **话题子系统现已迁到独立 `topic` 包**：话题工作记忆、摘要刷新、批量归属与候选评分已从 `agent` 包中拆出，主聊天 agent 只保留少量入口调用，后续维护 topic 逻辑时不再混在实时回复主链路里。
- **上下文分类现已改用结构化 JSON 输出而非工具调用**：回复前分类不再额外构造一次 forced tool call，而是按单次请求启用 `json_object` 响应格式，并基于结构体自动生成的 JSON Schema 约束输出后直接反序列化，减少纯结构化提取链路的额外工具开销。
- **多处单次结构化提取现已统一复用 JSON 输出 helper**：长期记忆 claim 提取、长期记忆合并判断、话题摘要生成和话题批量归属不再各自包一层单次工具调用，而是统一走 `json_object` 结构化输出与自动反序列化，减少样板代码和额外 agent/tool 开销。
- **机器人自述现已在上下文中标记为旧消息**：机器人自己刚说过的话进入后续上下文时会带上 `(OLD)`，降低模型把自述当成新输入继续接着复读的概率。
- **长期记忆候选现已减少显式跳过的噪声告警**：话题沉淀明确给出 `ignore` 时不再把正常跳过记录成无效 claim；未完事项的提示词也会更明确约束只允许输出目标型结果或忽略，减少后台反复刷无关告警。
- **消息清理与话题参与者查询现已避免整批保留 ID 和全量扫描**：旧消息清理不再拼接大 `NOT IN` 保留列表；话题参与者提取也不再一次性扫描整段话题历史，降低消息量上来后的清理和启动压力。

## [1.3.5] - 2026-05-07

### 修复

- **话题详情现已修正摘要变化与消息区的排版冲突**：摘要变化中的行内 diff 会在卡片内自然换行，不再冲出边框；未完事项与已确认事项也改为上下排布，长内容查看更顺手。
- **话题详情现已只保留一处关联消息并回到主内容区**：关联消息区不再重复出现，会放在左侧主内容流里并跟随整页滚动，避免被挤进右栏后影响阅读顺序。

## [1.3.4] - 2026-05-06

### 变更

- **话题摘要变化现已支持行内 diff 对比**：话题详情里的“最近摘要变化”会用红减绿加的方式直接标出标题和概括的改动，减少人工对照整段旧文和新文的成本。
- **摘要变化侧栏现已改成紧凑时间轴布局**：摘要快照较多时，右侧会使用可展开时间轴并限制滚动高度，展开内容也会保持更稳定的居中排布，减少左侧出现大段空白。
- **记忆列表现已支持查看全部状态与旧来源兼容筛选**：记忆页新增“全部状态”，旧版未标注 `status` 或 `source_kind` 的历史记录也能按来源线索正常显示和筛选。
- **长期记忆写入现已在入库时同步去重与合并**：主动记住和话题沉淀都会先做结构化判断，再直接写成生效记忆；事实、偏好、约束和目标在高相似时会合并，减少重复记忆持续堆积。
- **话题摘要刷新现会携带旧条目元数据继续生成**：摘要模型会尽量沿用既有 facts 和 open loops，而不是把同一件事拆成并列新条目。
- **默认人格提示词现已补充表情回复约束**：群友只发表情或表情包时，默认人格不再描述图片内容，而是更倾向于斗图或顺着聊天继续接话。
- **仓库内 JSON 编解码现已统一到 sonic**：后台提示载荷、健康检查和话题摘要读写共用同一套实现，外部字段语义保持不变，后续维护时不再混用两套序列化路径。

### 修复

- **话题摘要变化统计现已按真实内容变化计数**：仅刷新时间的快照会保留在时间轴里，但不会再和真正改动标题、概括或事项的快照混算在一起。
- **长期记忆去重现已避免跨主体误合并**：成员相关记忆在已知用户不同的情况下会保持分离，旧版事实键记录也会按正文继续兼容命中。
- **长期记忆向量同步失败现会显式返回错误**：创建、更新、删除和合并记忆时，如果向量同步失败，会直接回报失败并尽量恢复数据库侧状态，减少两边数据不一致。

## [1.3.3] - 2026-05-01

### 变更

- **后台筛选与摘要变化展示现已更直观**：记忆、话题、风格卡片和黑话页面的筛选项在桌面端会稳定显示在右上区域；话题详情里的“最近摘要变化”也改为只展示每次新增、移除或更新的部分，不再直接堆整段摘要快照。

### 修复

- **后台时间显示现已统一为可读文本**：话题详情、筛选相关页面和审核页面中的时间都会按统一格式展示，不再直接露出原始的 RFC3339 时间串。
- **记忆来源文案现已收口到真实链路**：记忆页来源筛选只保留“主动记住”和“话题沉淀”两类真实来源，并移除了不存在的“旧版迁入”来源文案，减少理解成本。

## [1.3.2] - 2026-04-29

### 变更

- **成员画像现已记录群名片与学习别称**：成员画像会为每位群友沉淀按群区分的历史群名片，以及从学习链路提炼出的稳定别称；后台成员页也新增了对应展示与搜索，便于核对“这个人平时都被怎么叫”。
- **渲染给 agent 的聊天称呼现已优先使用当前群名片**：普通聊天上下文、回复提示和“最近在场的人”会优先使用当前群可用的群名片；查不到时仅回退当前群历史群名片和原昵称，避免把别的群名片串到当前群。
- **查看成员信息时现会顺手校正 QQ 昵称**：当工具读取成员画像并拿到当前群最新 QQ 昵称时，如果发现数据库里的原昵称已过期，会自动回写为最新昵称，减少画像里昵称长期滞后的情况。

- **非 GLM 模型的群聊口吻现已增加少量正反例约束**：`persona` 提示词补充了短句短接、少解释、少客服腔的具体示例，并明确把复述截图、礼貌小作文和刷存在感短接列为应缩短或直接沉默的场景，降低 DeepSeek、Qwen 等模型回复偏话痨和偏服务化的问题。
- **人格静态提示词现已拆到独立模板文件（破坏性变更）**：`persona` 的静态 system prompt 与固定人设文案改为由 `config/persona.prompt` 统一维护，用户可以直接编辑该文件调整提示词正文；运行时仍从 `config.yaml` 读取 `persona` 的名称、QQ、别名和兴趣等程序配置，后台系统页也不再展示“人格简介”和“说话风格”两项，GitHub Release 打包产物与 `README` 说明也已同步包含该模板文件。
- **README 现已补充人格模板贡献说明**：在“快速开始”和 MCP 配置说明之间新增 `config/persona.prompt` 介绍，说明该文件用于调整人格提示词，并邀请测试出更好 prompt 的使用者通过 Issue 或 Pull Request 贡献改进。
- **从源码构建现已要求 Go 1.26**：`go.mod`、依赖版本和 `README` 中的构建说明已同步升级；仍使用 Go 1.25 或更早版本时，当前代码将无法按最新依赖要求完成编译。
- **话题分配工具现已统一使用 `reuse` 归入已有话题**：模型侧不再区分 active 的 `reuse` 和 archived 的 `reopen`，归档候选会在后端按需要自动重新激活，减少话题分配动作判断的分叉。

### 修复

- **群聊里的 @ 群友现已优先显示群名片或昵称**：OneBot 在解析入站群消息时会通过 `get_group_member_info` 补全普通 at 的群名片或昵称，并做短期缓存；查询失败时仍会保留 QQ 号，减少对话上下文里难以辨认的数字提及。

## [1.3.1] - 2026-04-27

### 变更

- **协作约束现已补充核心架构边界**：`AGENTS.md` 新增话题主路径、学习系统职责、原始文本语义边界、“宁缺毋滥”质量取向与消息保留规则，后续协作默认按这些约束执行。
- **README 现已校准当前能力描述**：补充话题工作记忆、三档模型和管理后台说明，并修正 Milvus 依赖、后台访问等运行说明。
- **话题归属现已改为后台批量处理**：消息会先快速写入记录，再由后台按群攒批调用模型分配话题；低信息消息可以保持无话题，话题摘要也会异步刷新，减少聊天回复被话题整理阻塞的情况。
- **回复前分类现已同时服务语气和检索**：机器人每轮思考只做一次上下文分类，用同一结果选择发言风格并生成话题检索词；分类失败或检索词为空时会跳过归档话题和主动长期记忆检索。
- **学习系统现已改为只消费话题系统已处理消息**：learner 只学习黑话、风格卡片和全局成员画像，并按连续已处理前缀推进水位；未归属话题的 pending 消息会留给话题系统先处理，不再与长期记忆下沉链路重复扫原文。
- **关停后的待分配消息现已改为启动补偿**：关闭时不再把未分配消息伪造为无话题，保留真实 pending 状态；下次启动会扫描历史 pending 并强制补跑一次话题归属，即使未满批次也会处理。
- **仓库现已清理未引用实现与全部测试代码**：移除了仓库内已不再被主线调用的冗余函数，并删除现有 Go 测试文件，后续 `go test ./...` 将主要作为编译级检查使用。

### 修复

- **群聊提及现已按消息段显示昵称**：收到 at 群友的消息时，若 OneBot 消息段提供可读昵称，机器人上下文会显示昵称；无法识别昵称时保留 QQ 号，避免误指代其他群友。
- **话题分配结果现已可区分**：消息记录会通过话题编号和分配原因区分待处理、已归属、已处理但无话题或处理失败，避免队列满、模型失败等情况被误看作仍在等待自动处理。
- **归档话题摘要向量现已按最新摘要维护**：活跃话题被归档让位后会先移除旧向量并触发后台摘要刷新，摘要推进后再写入新的归档话题向量，减少历史话题召回使用过期摘要。
- **长期记忆向量现已跟随状态变化**：候选和归档记忆会移除向量，活跃、旧版兼容记忆才参与向量召回；向量结果不足时会用关键词检索补足，降低不可用记忆挤占召回名额的概率。
- **长期记忆去重现已避免重复调用模型**：同值判断只使用已有正文和本次提取出的值摘要做确定性比较，不再为了比较旧记忆额外触发一次模型调用。
- **话题分配原因现已保留模型原文**：模型给出的 `reason` 会直接持久化，只有缺失时才回落到稳定码，便于区分真实语义 `no_topic` 与队列满、异常等操作性 fallback。
- **消息清理现已保护已分配话题和 learner 未处理消息**：消息清理除了保留最新消息外，也会保留所有已归属话题消息和 learner 水位之后的消息，避免话题上下文或未学习内容被提前清掉。

## [1.3.0] - 2026-04-26

### 新增

- **话题工作记忆系统现已成为主路径**：机器人会持续识别群聊中的当前话题、维护话题摘要、记录未完事项，并在后续回复中带入更稳定的话题上下文。
- **管理后台现已支持话题归档与回看**：可以查看进行中和已归档话题，按群号、状态、关键词筛选，并进入详情查看摘要变化和关联消息。
- **机器人现已具备主动整理话题摘要的工具**：对话过程中可以主动整理当前话题，并把后续可复用的信息沉淀到话题与长期记忆中。
- **长期记忆现已引入声明提取与规范化流程**：新增事实、经历、偏好、约束、目标等归类，并记录来源、状态、证据数量和事实键，减少重复与低可信记忆。
- **已归档话题现可参与向量召回**：新增话题摘要向量集合，后续对话可以从历史话题中召回相关上下文。
- **管理后台记忆页现已支持归档、恢复和删除操作**：列表可以按状态、归类、来源和关键词筛选，便于清理过期或不可信记忆。
- **根目录现已新增发布更新日志**：`CHANGELOG.md` 将作为后续 GitHub Release 的发布说明来源。

### 变更

- **模型配置现已统一为高、中、低三档（破坏性变更）**：高档模型负责实时思考与回复，中档模型负责话题摘要、后台学习和记忆合并，轻量模型负责群风格判定；升级时需要把旧的 `llm`、`auxiliary_model`、`style_classification_model` 配置迁移到 `model_tiers.high`、`model_tiers.mid`、`model_tiers.low`。
- **模型 API Key 环境变量现已按三档模型命名（破坏性变更）**：配置项调整为 `MUMU_MODEL_HIGH_API_KEY`、`MUMU_MODEL_MID_API_KEY`、`MUMU_MODEL_LOW_API_KEY`，旧的 `MUMU_LLM_API_KEY`、`MUMU_AUX_LLM_API_KEY`、`MUMU_STYLE_CLASSIFICATION_API_KEY` 不再作为模型配置读取。
- **机器人回复提示词现已接入更多上下文来源**：话题记忆、相关长期记忆、跨群经历和最近发言者画像会共同参与回复构建，减少脱离上下文的回答。
- **长期记忆检索现已优先生效中的记忆**：旧版记录仍会兼容读取，但排序上优先使用已经确认有效的记忆。
- **管理后台记忆页现已展示更完整的判断信息**：状态、归类、来源和证据数会直接显示，便于判断记忆是否可信、是否需要归档。
- **发布流程现已改为读取根目录更新日志**：GitHub Release 会从 `CHANGELOG.md` 提取对应版本段落，缺少版本段落时会直接失败。
- **协作规范现已要求同步维护更新日志**：Agent 完成会进入仓库的改动时，需要同步记录面向使用者和维护者的更新内容。
- **更新日志格式现已统一为中文分组与加粗摘要式条目**：每条记录先给出加粗结论，再补充影响范围和具体说明，便于发布时直接阅读。

### 修复

- **管理后台状态筛选按钮现已可正常点击**：修复风格卡片和黑话列表中的状态筛选控件无法点击的问题。
- **话题写入异常现已不再中断收消息主流程**：话题工作记忆写入失败时会记录错误，但仍会保留运行时消息缓冲、继续成员画像更新与后续思考调度，避免临时存储问题导致机器人不响应。
- **主动长期记忆检索现已只使用话题模块生成的检索词**：话题上下文构建失败或未生成检索词时会跳过本轮主动召回，不再把聊天展示文本硬塞进长期记忆检索。
- **检索和学习文本现已避免使用消息渲染元信息**：话题匹配、归档话题召回、主动长期记忆检索、黑话匹配、风格分类、话题摘要和成员画像学习只使用原始文本；原始文本为空的图片、表情包或纯媒体消息会跳过这些语义处理，减少时间、消息 ID、昵称等渲染元信息对结果的干扰。

## [1.2.4] - 2026-04-12

### 修复

- **后台 Toast 样式现已恢复正常**：修复提示信息显示异常的问题。
